enum StatusApp {
  SUCCESS,
  ERROR,

  CONNECT_SEVER_FAIL,
  REQUEST_RESET,
}

enum MessageType {
  SUCCESS,
  ERROR,
  NOTICE
}

enum ItemType {
  FOOD,
  SNACK,
  ENTERTAINMENT,
  THING,
  HOME,
  TRAVEL,
  OTHER
}

enum UseType {
  TOGETHER,
  SEPARATELY
}

enum Member {
  TUNG,
  TRUC
}